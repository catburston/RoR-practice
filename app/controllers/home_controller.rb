class HomeController < ApplicationController
  def welcome
    #as the method name is the same as the control, there is no need to render
    #render "welcome"
  end

  def contact
    #render "contact"
  end
end
